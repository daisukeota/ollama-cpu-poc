version: '3.8'

services:
  ollama-cpu:
    build: .
    container_name: ollama-cpu
    ports:
      - "11434:11434"
    volumes:
      - ollama_cpu_storage:/root/.ollama
    restart: unless-stopped

volumes:
  ollama_cpu_storage: